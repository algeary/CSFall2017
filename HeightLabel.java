import javax.swing.tree.TreeNode;

public class HeightLabel {
        public TreeNode rewire(TreeNode t) {
            // replace with working code
        	int[] vals = inOrder(t.next);
            return null;
            
        }
    }